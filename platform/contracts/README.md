# Shared Contracts

这个目录预留给：

1. OpenAPI 契约
2. 内部 REST Schema
3. 未来的 protobuf / gRPC 契约
4. 前后端共享的错误码、枚举和模型定义

建议后续拆分：

1. `contracts/openapi/public/`
2. `contracts/openapi/internal/`
3. `contracts/errors/`
4. `contracts/events/`
