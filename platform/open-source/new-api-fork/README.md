# new-api-fork Placeholder

这个目录只是当前 monorepo 中的占位说明，不建议把真正的 `new-api-fork` 长期和闭源代码放在同一个仓库里。

推荐做法：

1. 真正的 `new-api-fork` 放在独立公开仓库
2. 通过 Git 子模块、release artifact 或镜像版本来和闭源平台对接
3. 所有商业逻辑继续留在闭源服务，不进入 fork
