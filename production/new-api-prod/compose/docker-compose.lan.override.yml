services:
  new-api:
    ports:
      - "0.0.0.0:${NEW_API_PORT}:3000"
  mysql:
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --skip-name-resolve
      - --max_connections=300
      - --innodb-buffer-pool-size=512M
